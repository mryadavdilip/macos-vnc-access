# macOS Remote Access via GitHub Actions

This repository provides remote VNC access to a macOS machine via GitHub Actions.

## Quick Start

The workflow is already running. Check the Actions tab for connection details.

## Files
- `.github/workflows/macos-vnc.yml` - GitHub Actions workflow for macOS VNC
- `allocate-mac-host.ps1` - AWS EC2 Mac host allocation script (backup option)
